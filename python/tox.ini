[tox]
env_list = py310, py311, py312, py313, py314
isolated_build = True

[testenv]
runner = uv-venv-lock-runner
dependency_groups = dev
commands =
    pytest --cov=scopemask --cov-report=term-missing {posargs}
